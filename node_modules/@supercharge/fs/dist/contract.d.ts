import Fs from 'fs-extra';
export interface FsContract {
    /**
     * Returns the file size in bytes of the file located at `path`.
     *
     * @param {String} path
     *
     * @returns {Integer}
     */
    size: (path: string) => Promise<number>;
}
export declare type SuperchargeFs = typeof Fs & FsContract;
